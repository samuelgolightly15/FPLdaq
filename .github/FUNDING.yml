# Puts a "Sponsor" button at the top of the repository page.
# Replace YOUR_KOFI_HANDLE with the handle chosen when setting up Ko-fi,
# i.e. the part after ko-fi.com/ in your page URL. Delete the line until
# then: an unresolved handle renders a broken button.
github: [samuelgolightly15]
ko_fi: marcusleadboot
